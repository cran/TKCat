% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClickHouse.R
\name{ch_join_query}
\alias{ch_join_query}
\title{Build a ClickHouse \code{JOIN} query from two sub-queries}
\usage{
ch_join_query(q1, q2, using, type = "LEFT", select = "*", a1 = "l", a2 = "r")
}
\arguments{
\item{q1}{a character string with the left-hand side \code{SELECT} sub-query}

\item{q2}{a character string with the right-hand side \code{SELECT} sub-query}

\item{using}{a character string with the column(s) to join on, as expected
after the \code{USING} keyword (e.g. \code{"id"} or \code{"(id, version)"})}

\item{type}{the join type, passed verbatim before the \code{JOIN} keyword
(e.g. \code{"LEFT"}, \code{"INNER"}, \code{"FULL"}, \code{"ANY LEFT"}). Default: \code{"LEFT"}.}

\item{select}{a character string with the columns to select. Default:
\code{"*"}. Columns can be qualified with the sub-query aliases (see \code{a1}/\code{a2})
to disambiguate non-key columns present on both sides.}

\item{a1}{alias given to the left-hand side sub-query. Default: \code{"l"}.}

\item{a2}{alias given to the right-hand side sub-query. Default: \code{"r"}.}
}
\value{
A character string with the assembled \code{JOIN} query.
}
\description{
Wraps two SQL sub-queries in a single \verb{SELECT ... JOIN ... USING ...}
statement. Both sub-queries are used as derived tables, so they can be any
valid \code{SELECT} statements.
}
\seealso{
\code{\link[=ch_union_query]{ch_union_query()}}
}
