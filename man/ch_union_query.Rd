% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClickHouse.R
\name{ch_union_query}
\alias{ch_union_query}
\title{Build a ClickHouse \code{UNION} query from several sub-queries}
\usage{
ch_union_query(queries, mode = c("DISTINCT", "ALL"))
}
\arguments{
\item{queries}{a character vector of \code{SELECT} sub-queries to combine}

\item{mode}{the union mode: \code{"DISTINCT"} (default) to deduplicate rows
across sub-queries, or \code{"ALL"} to keep every row}
}
\value{
A character string with the assembled \code{UNION} query.
}
\description{
Wraps each input sub-query as a derived table and combines them with
\verb{UNION DISTINCT} or \verb{UNION ALL}. A single sub-query is returned wrapped but
without any \code{UNION} clause.
}
\seealso{
\code{\link[=ch_join_query]{ch_join_query()}}
}
